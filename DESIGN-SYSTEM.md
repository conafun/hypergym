# HyperGym 设计语言（从参考图提取）

> 来源：`ui-refs/` 下三张参考图。色值由像素级采样提取（量化到 16 级再统计占比），
> 结构由 modlens 视觉桥转写。本文档是 UI/UX 阶段的配色与结构"素材库"，
> 落地到 Compose 时从这里取 token，不改动数据层/联通层。

---

## 1. 三张参考图的精确色板

### 图 1 · 健身 App（1.webp，1504×1128）—— 最相关
| 色值 | 占比 | 用途 |
|---|---|---|
| `#F0F0F0` | 31% | 卡片面/浅底 |
| `#E0E0E0` | 33% | 背景灰 |
| `#D0D0D0` | 25% | 分隔/浅描边 |
| **`#E06040`** | ~5% | **主强调·暖橙**（进度条、快捷入口、按钮） |
| `#303030`~`#505050` | ~1% | 文字 |

结构：①首页「问候 + 大目标进度条 + 四宫格快捷入口」②周报「分段切换 + 双圆环 + 步数距离 + 贴士卡」③AI 对话页。
风格：大圆角、柔和阴影、新拟态（neumorphism）。

### 图 2 · 电商 App（56930，1024×683）
| 色值 | 占比 | 用途 |
|---|---|---|
| `#B0E0F0` | 54% | 背景浅青（展示底） |
| `#F0F0F0` | 20% | 卡片白 |
| **`#1080F0`→`#2090F0`→`#40C0F0`** | ~4% | **青蓝渐变**（主按钮/header） |
| **`#50D0A0`** | ~1% | **青绿**（次强调） |

结构：商品卡流 / 登录大留白 / 支付分步。风格：玻璃拟态 + 渐变按钮。

### 图 3 · SplashID 仪表盘（9929629，1024×716）
| 色值 | 占比 | 用途 |
|---|---|---|
| `#D0D0E0` | 32% | 背景浅薰衣草灰 |
| `#F0F0F0` | 19% | 卡片白 |
| **`#402070`** | ~1% | **深紫**（顶部渐变 header、品牌） |
| **`#7090E0`** / `#80A0F0` | ~1% | **长春花蓝**（图表柱） |
| `#50D0C0` | ~0.3% | 青（辅助） |

结构：顶部渐变 header + 大数字(395 IDs) + 6 个月柱状图 + 指标卡(48分类/48类型) + **健康清单（进度条列表）** + 备份状态条。

---

## 2. 统一设计语言（拟落地 token）

**主色（品牌强调·运动风）**
- Primary 暖橙 `#E06040`：大数字、主按钮、进度、PR 徽章、选中态
- Primary 深 `#C84E32`：按压态 / 渐变尾

**图表/辅助色**
- Series 蓝 `#7090E0`：趋势线、肌群条、周报柱
- Series 青绿 `#50D0A0`：达标/容量、正向指标
- Hero 深紫 `#402070`：可选，顶部渐变 header / 深色模式主色（工具感）

**中性色**
- 背景 `#F6F7F8`（微暖灰）
- 卡片 `#FFFFFF`
- 描边/分隔 `#E6E6E6`
- 文字主 `#2B2B2B`、次 `#757575`

**形状**
- 卡片圆角 16dp（大卡片）/ 24dp（按钮，已沿用）
- 柔和阴影：`alpha 0.06`，y 偏移 2~4dp
- 数字层级：Hero 大数字 34~40sp 加粗，标签 12sp，正文 15sp

---

## 3. 结构模式 → 我们 App 的映射

| 我们的页面 | 借鉴来源 | 落地要点 |
|---|---|---|
| 数据总览(首页) | 图1首页 + 图3 header | 顶部渐变 header + 大数字总容量 + 分段切换 + 指标卡（我们已有 HeroCard/RangeSelector，换配色+渐变头） |
| 肌群分布 | 图1双圆环 / 图3柱状图 | 环形肌群图或横向柱条，每肌群一个 Series 色 |
| 训练日记 | 图3健康清单 | 每次训练一行 + 进度条（组数/容量完成度）+ PR 徽章 |
| 动作详情页(未来) | 图1周报 / 图3柱状图 | e1RM 力量曲线（蓝）+ 历史重量表 |
| 设置/登录(未来) | 图2 | 玻璃拟态大留白 + 渐变按钮 |
| AI 教练(未来) | 图1对话页 | 气泡对话 + 底部动作按钮 |

---

## 4. 待定项（下一轮确认）

- 主色方向：**暖橙 `#E06040`（运动风，推荐）** vs 深紫 `#402070`（工具/金融感）
- 是否引入顶部渐变 header（图3）替换现在的纯色 HeroCard
